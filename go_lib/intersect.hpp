#pragma once
#include <iostream>
#include <vector>
#include <cmath>

#include "go_lib.hpp"

namespace go
{
    bool is_node_inside(Vertex vert, Node node);
    bool is_node_inside_trian(Vertex vert, Node node);
} // namespace go
